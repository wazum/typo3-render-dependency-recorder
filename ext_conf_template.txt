# cat=recorder; type=string; label=Record header name
recordHeader = X-Render-Record

# cat=recorder; type=string; label=Run id header name
runHeader = X-Render-Run

# cat=recorder; type=string; label=Comma-separated application contexts in which recording is allowed (prefix match)
activeContexts = Testing

# cat=recorder; type=string; label=Comma-separated project roots captured paths are filtered to (repo-relative prefixes)
roots = source/,local/

# cat=recorder; type=string; label=Header that requests deep (executed-PHP) capture when its value is "deep"
depthHeader = X-Render-Depth

# cat=recorder; type=string; label=Directory the per-request files are written to (absolute, or relative to the project root); empty = var/render-dependency-recorder
outputDirectory =

# cat=recorder; type=string; label=Absolute path recorded file paths are made relative to; empty = the composer project root
projectRoot =
