name: render-dependency-recorder

project_files:
  - commands/host/render-record

post_install_actions:
  - echo "render-dependency-recorder add-on installed. Run 'ddev render-record <url>' for a deep (Xdebug coverage) record."
